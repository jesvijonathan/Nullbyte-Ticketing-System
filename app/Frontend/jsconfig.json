{
  "compilerOptions": {
    "baseUrl": "./", // Sets the root directory
    "paths": {
      "@/*": ["./src/*"] // Maps "@" to "./src" for cleaner imports
    }
  },
  "exclude": ["node_modules", "dist"] // Excludes unnecessary folders
}
